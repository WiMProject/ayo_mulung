name: ayo_mulung
description: "A new Flutter project."

publish_to: "none" # Prevent publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Karena semua gambar dari internet dan font default, tidak ada assets/font lokal
  # Jika nanti perlu assets lokal atau font, tambahkan di sini
